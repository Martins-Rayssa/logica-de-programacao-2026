import leia from "readline-sync"

var quilo = leia.questionFloat("digite a distancia de quilometros percorridos: ");
var liquido = leia.questionFloat("digite a quantidade de litros em combustivel gasto: ");
 
var calculo = quilo / litos;
console.log("o consumo medio de km/1 é: " + calculo.toFixed(2))
